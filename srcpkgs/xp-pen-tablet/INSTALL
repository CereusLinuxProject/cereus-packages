# INSTALL

case "$ACTION" in
post)
        # Restore permissions for some pentablet files, as they must be r/w by all users. Otherwise, pentablet will not be able to save configurations.
        chmod +0666 /usr/lib/pentablet/resource.rcc
        chmod +0666 /usr/lib/pentablet/conf/xppen/*
    ;;

esac
